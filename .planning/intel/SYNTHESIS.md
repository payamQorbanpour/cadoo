# Synthesis Summary

Single entry point for downstream consumers (e.g. gsd-roadmapper). Generated by gsd-doc-synthesizer.

- Mode: merge (2026-06-14) — added the CI-mode dedup convergence SPEC into existing intel.
  Prior ingest was mode `new` (2026-06-04 release-docs SPEC).
- Precedence: ADR > SPEC > PRD > DOC (no per-doc overrides; both docs carry `precedence: null`).

## This merge (2026-06-14)

New doc synthesized:
- /Users/payam/projects/github.com/payamqorbanpour/cadoo/docs/superpowers/specs/2026-06-14-cadoo-ci-dedup-convergence-design.md
  (SPEC, confidence: high, locked: false) — "Convergent CI-mode review (dedup that reaches a fixed point)"

Targets a **distinct subsystem** from the prior release-docs SPEC: `cadoo-cli` CI-mode stateless dedup
(`internal/orchestrator`, `internal/findings`, `internal/vcs`). No scope overlap with `internal/releasedocs`.

## Doc counts by type (cumulative across intel)

- SPEC: 2 (release-docs 2026-06-04; CI-dedup-convergence 2026-06-14)
- ADR: 0
- PRD: 0
- DOC: 0
- UNKNOWN/low-confidence: 0

Total docs in classifications dir: 2 (both SPEC, both high confidence).

## Cross-ref / cycle detection

Run. The new doc's `cross_refs` array is empty; the prior doc's cross_refs point only to code modules/files.
The cross-doc graph is empty — no cycles, no traversal-depth concern.

## Decisions locked

- **0 locked.** No ADRs in either ingest. All design decisions are recorded as **proposed (SPEC-origin,
  not locked)** in `decisions.md`. Downstream consumers must not treat them as immutable.
- New this merge: DEC-cidedup-carry-structural-key, DEC-cidedup-resolved-as-durable-memory,
  DEC-cidedup-incremental-dual-context, DEC-cidedup-resolvestale-scoped-to-changeset,
  DEC-cidedup-no-db-migration.

## Requirements extracted

- 10 total (all SPEC-origin; no PRDs present). See `requirements.md`.
  - Release-docs (6): REQ-release-artifact-generation, REQ-per-artifact-toggles, REQ-configurable-templates,
    REQ-release-docs-idempotency, REQ-configurable-trigger, REQ-publish-destinations.
  - CI-dedup (4, new this merge): REQ-cidedup-convergent-review, REQ-cidedup-no-self-resolution,
    REQ-cidedup-honor-resolves, REQ-cidedup-incremental-review.

## Constraints

- 14 total. See `constraints.md`. New this merge (6, CI-dedup):
  - api-contract: 2 (CONS-cidedup-self-resolution-fix Part A; CONS-cidedup-data-marker-changes)
  - protocol: 3 (CONS-cidedup-thread-state-suppression Part B; CONS-cidedup-incremental-review Part C;
    CONS-cidedup-resolvestale-incremental-rule)
  - nfr: 1 (CONS-cidedup-nonfunctional — convergence invariant + YAGNI out-of-scope)
- Prior release-docs constraints (8) unchanged.

## Context topics

- 4 total. See `context.md`. New this merge (2): CI-dedup open implementation questions; CI-dedup codebase
  grounding (concrete code locations to read first).

## Conflicts

- Blockers: 0
- Competing variants: 0
- Auto-resolved: 0

Detail: /Users/payam/projects/github.com/payamqorbanpour/cadoo/.planning/INGEST-CONFLICTS.md

## Per-type intel files

- /Users/payam/projects/github.com/payamqorbanpour/cadoo/.planning/intel/decisions.md
- /Users/payam/projects/github.com/payamqorbanpour/cadoo/.planning/intel/requirements.md
- /Users/payam/projects/github.com/payamqorbanpour/cadoo/.planning/intel/constraints.md
- /Users/payam/projects/github.com/payamqorbanpour/cadoo/.planning/intel/context.md

## Note for the roadmapper

The CI-dedup SPEC is "Approved design, pending implementation plan" — it ships in three independently
verifiable parts (A: stop self-resolution; B: durable resolved-thread suppression; C: incremental review as
a growth ceiling), built in that order. `constraints.md` is the authoritative technical contract; the
convergence invariant (CONS-cidedup-nonfunctional) is the acceptance gate: once code stops changing, thread
count is monotonic non-increasing, and a re-run on an unchanged head posts/resolves zero. This is a separate
concern from the active v2.0 MCP milestone and from the shipped release-docs milestones — route it as its own
unit of work. Read `.planning/codebase/*` plus the code locations listed in `context.md` for grounding.
