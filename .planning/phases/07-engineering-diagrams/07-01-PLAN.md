---
phase: 07-engineering-diagrams
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/releasedocs/releasedocs.go
  - internal/config/config.go
  - .cadoo.yaml.example
autonomous: true
requirements: [DIAG-01]
must_haves:
  truths:
    - "A user can set releaseDocs.artifacts.diagrams.enabled and per-type path lists in .cadoo.yaml (D-01, D-04, D-07)"
    - "An empty/absent diagram type key carries no paths and produces nothing for that type (D-04)"
    - "The diagrams family is gated by one enabled + when: condition, not per-type toggles (D-07)"
  artifacts:
    - path: "internal/releasedocs/releasedocs.go"
      provides: "KindDiagrams ArtifactKind constant"
      contains: "KindDiagrams ArtifactKind = \"diagrams\""
    - path: "internal/config/config.go"
      provides: "DiagramsConfig struct with five per-type []string fields + ArtifactConfig inline-embed, wired into ReleaseArtifacts"
      contains: "Diagrams DiagramsConfig"
    - path: ".cadoo.yaml.example"
      provides: "documented diagrams: block under releaseDocs.artifacts"
      contains: "diagrams:"
  key_links:
    - from: "internal/config/config.go ReleaseArtifacts"
      to: "DiagramsConfig"
      via: "Diagrams field with yaml:\"diagrams\" tag"
      pattern: "Diagrams DiagramsConfig"
    - from: "DiagramsConfig"
      to: "config.ArtifactConfig"
      via: "yaml:\",inline\" embed (single enabled + when gate)"
      pattern: "ArtifactConfig `yaml:\",inline\"`"
---

<objective>
Establish the contract/config layer for the engineering-diagrams generator: the `KindDiagrams` artifact-kind constant and the `DiagramsConfig` struct (five per-type Mermaid path lists + an inline-embedded `ArtifactConfig` family gate), plus user-facing documentation in `.cadoo.yaml.example`. This is the foundation that the generator package (plan 02) and wiring (plan 03) build on.

Purpose: DIAG-01 — a user can enable a `diagrams` release-docs artifact and choose which diagram types are produced (sequence, dependency, state, flowchart, class) via `.cadoo.yaml`, gated by the existing `enabled:`/`when:` toggles like every other artifact (D-07). Mirrors the Phase 3 `APIDocsConfig`/`KindAPIDocs` pattern exactly.
Output: One new const, one new struct + one new field, one documented YAML block. No behavior yet — those are pure declarations the generator reads.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-engineering-diagrams/07-CONTEXT.md
@.planning/phases/07-engineering-diagrams/07-RESEARCH.md
@.planning/phases/07-engineering-diagrams/07-PATTERNS.md

<interfaces>
<!-- Exact analog declarations the executor mirrors. Extracted from the codebase. -->

From internal/releasedocs/releasedocs.go:20-36 (the const block KindDiagrams is appended to):
```go
const (
	KindChangelog    ArtifactKind = "changelog"
	KindReleaseNotes ArtifactKind = "release_notes"
	KindBlog         ArtifactKind = "blog"
	// KindAPIDocs ... (immediately above where KindDiagrams goes)
	KindAPIDocs ArtifactKind = "apidocs"
)
```

From internal/config/config.go:75-88 (ReleaseArtifacts — add the Diagrams field after the APIDocs field at line 88):
```go
type ReleaseArtifacts struct {
	Changelog    ArtifactConfig     `yaml:"changelog"`
	ReleaseNotes ReleaseNotesConfig `yaml:"releaseNotes"`
	Blog         ArtifactConfig     `yaml:"blog"`
	APIDocs      APIDocsConfig      `yaml:"apiDocs"`
}
```

From internal/config/config.go:118-132 (APIDocsConfig — the inline-embed pattern DiagramsConfig mirrors):
```go
type APIDocsConfig struct {
	ArtifactConfig `yaml:",inline"`
	SpecPath       string `yaml:"specPath"`
}
```

From internal/config/config.go:94-107 (ArtifactConfig — the embedded family gate; carries Enabled + When + Preset + Template):
```go
type ArtifactConfig struct {
	Enabled  bool   `yaml:"enabled"`
	When     string `yaml:"when"`
	Preset   string `yaml:"preset"`
	Template string `yaml:"template"`
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add KindDiagrams artifact-kind constant</name>
  <read_first>
    - internal/releasedocs/releasedocs.go:20-36 (the recognized-artifact-kinds const block; mirror the KindAPIDocs entry at lines 29-35)
    - .planning/phases/07-engineering-diagrams/07-PATTERNS.md (section "internal/releasedocs/releasedocs.go (model/const) — MODIFY", lines 240-252 — exact docstring text)
    - .planning/phases/07-engineering-diagrams/07-RESEARCH.md (section "KindDiagrams constant", lines 343-352)
  </read_first>
  <action>
    Append a `KindDiagrams ArtifactKind = "diagrams"` entry to the existing const block in internal/releasedocs/releasedocs.go (immediately after the `KindAPIDocs` entry, inside the same `const ( … )` at lines 20-36). This is the ONLY edit to this file — `MultiGenerator`, `Artifact.Filename`, and `FileFetcher` already exist from Phase 3, so do NOT add or alter any interface. Give the constant a docstring (revive `exported` is ON — CLAUDE.md): describe that a single `GenerateMulti` call emits one markdown-page `Artifact` per resolved Mermaid source, each differentiated by its `Filename` field (e.g. `"diagrams/sequence/login.md"`), and that the diagrams generator implements the `MultiGenerator` interface. Do not touch any other file in this task.
  </action>
  <verify>
    <automated>grep -q 'KindDiagrams ArtifactKind = "diagrams"' internal/releasedocs/releasedocs.go && go build ./internal/releasedocs/...</automated>
  </verify>
  <acceptance_criteria>
    - `internal/releasedocs/releasedocs.go` contains the literal `KindDiagrams ArtifactKind = "diagrams"` inside the recognized-kinds const block.
    - The constant has a preceding `// KindDiagrams …` docstring comment.
    - `go build ./internal/releasedocs/...` succeeds.
    - No interface (`MultiGenerator`, `FileFetcher`, `Artifact`) was modified — `git diff internal/releasedocs/releasedocs.go` shows only the new const lines.
  </acceptance_criteria>
  <done>KindDiagrams is declared and the package compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Add DiagramsConfig struct and wire it into ReleaseArtifacts</name>
  <read_first>
    - internal/config/config.go:75-88 (ReleaseArtifacts struct — add the Diagrams field after the APIDocs field at line 88)
    - internal/config/config.go:118-132 (APIDocsConfig — the inline-embed analog to mirror)
    - internal/config/config.go:94-107 (ArtifactConfig — the embedded family gate, carrying enabled/when/preset/template)
    - .planning/phases/07-engineering-diagrams/07-PATTERNS.md (section "internal/config/config.go (config) — MODIFY", lines 255-279 — exact field set + docstring guidance)
    - .planning/phases/07-engineering-diagrams/07-RESEARCH.md (section "DiagramsConfig (mirror APIDocsConfig exactly)", lines 317-341)
  </read_first>
  <action>
    Two edits to internal/config/config.go. (1) Add a `Diagrams DiagramsConfig \`yaml:"diagrams"\`` field to `ReleaseArtifacts` (after the `APIDocs` field, line 88) with a docstring noting the user lists committed Mermaid source paths per diagram type and all output is gated together by a single enabled + when: condition (D-07), wired in Phase 7. (2) Add a new exported `DiagramsConfig` struct mirroring `APIDocsConfig`: embed `ArtifactConfig \`yaml:",inline"\`` (the single family gate — D-07), then five `[]string` fields with these exact names and yaml tags: `Sequence \`yaml:"sequence"\``, `Dependency \`yaml:"dependency"\``, `State \`yaml:"state"\``, `Flowchart \`yaml:"flowchart"\``, `Class \`yaml:"class"\``. The five types are FIXED (D-04). Note in the struct docstring that the embedded `ArtifactConfig` carries `Preset`/`Template` but the diagrams generator IGNORES them in v1 (RESEARCH Q2 — fixed wrapper). Every exported symbol AND every exported field needs a docstring (revive `exported` is ON). Do NOT add per-type enabled/when toggles — one family gate only (D-07). Do not touch any other file.
  </action>
  <verify>
    <automated>grep -q 'Diagrams DiagramsConfig' internal/config/config.go && grep -q 'type DiagramsConfig struct' internal/config/config.go && go build ./internal/config/...</automated>
  </verify>
  <acceptance_criteria>
    - `internal/config/config.go` contains `Diagrams DiagramsConfig \`yaml:"diagrams"\`` inside `ReleaseArtifacts`.
    - `internal/config/config.go` contains `type DiagramsConfig struct` with an inline-embedded `ArtifactConfig` and exactly five `[]string` fields: `Sequence`, `Dependency`, `State`, `Flowchart`, `Class` with matching lowercase yaml tags.
    - No per-type `enabled`/`when` field exists on `DiagramsConfig` (single family gate, D-07).
    - `go build ./internal/config/...` succeeds and `make lint` reports no missing-docstring errors for the new symbols.
  </acceptance_criteria>
  <done>DiagramsConfig exists with five fixed per-type path lists, gated by one inline ArtifactConfig, and is reachable via cfg.Artifacts.Diagrams.</done>
</task>

<task type="auto">
  <name>Task 3: Document the diagrams block in .cadoo.yaml.example</name>
  <read_first>
    - .cadoo.yaml.example:106-124 (the apiDocs: block + its explanatory comment — the sibling to mirror; add the diagrams: block immediately after, before the `publish:` block at line 126)
    - .planning/phases/07-engineering-diagrams/07-PATTERNS.md (section ".cadoo.yaml.example (config doc) — MODIFY", lines 324-343 — the exact block to add)
  </read_first>
  <action>
    Add a `diagrams:` block inside `releaseDocs.artifacts:` (after the `apiDocs:` block, before `publish:`). Mirror the apiDocs comment-then-block style. Keys: `enabled: false`, `when: always` (default — runs on every release, D-08), then the five per-type path lists `sequence: []`, `dependency: []`, `state: []`, `flowchart: []`, `class: []`, each with an inline comment naming the Mermaid keyword(s) that type accepts (sequence→sequenceDiagram; dependency→graph/flowchart/erDiagram; state→stateDiagram; flowchart→flowchart/graph; class→classDiagram). Above the block, add a comment explaining: it renders committed Mermaid sources (one markdown page per source, wrapped in a mermaid fence) published to pages at `releases/<tag>/diagrams/<type>/<name>.md`; a missing or non-Mermaid source is skipped with a logged reason without failing siblings (D-08); and the Pages-rendering nuance per RESEARCH Q1 — a mermaid fence renders on github.com's file view and release bodies, while a Jekyll/GitLab-Pages *served* site needs a client-side mermaid include (deferred). Match the file's existing two-space indentation. Do not touch any other file.
  </action>
  <verify>
    <automated>grep -q '^    diagrams:' .cadoo.yaml.example && grep -A8 '^    diagrams:' .cadoo.yaml.example | grep -q 'sequence:' && grep -A8 '^    diagrams:' .cadoo.yaml.example | grep -q 'class:'</automated>
  </verify>
  <acceptance_criteria>
    - `.cadoo.yaml.example` has a `diagrams:` block under `releaseDocs.artifacts:` containing `enabled:`, `when:`, and all five per-type keys (`sequence`, `dependency`, `state`, `flowchart`, `class`).
    - A preceding comment documents the mermaid-fence render behavior and the per-source graceful-skip (D-08).
    - The YAML still parses: `go test ./internal/config/...` (any existing example-load test) stays green, or a manual `yaml` lint of the file succeeds.
  </acceptance_criteria>
  <done>The diagrams artifact is documented for users with per-type selection and the Pages-rendering caveat.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.cadoo.yaml` (repo head) → config structs | User-/maintainer-supplied config values (paths, toggles) deserialize into `DiagramsConfig`. Same trust level as any committed repo file. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | `DiagramsConfig` per-type path lists (`Sequence`/…/`Class`) | accept | Paths are plain `[]string` here — they carry no behavior in this plan. Path-traversal risk is handled downstream where `Filename` is derived (plan 02, via `path.Base`) and where the pages publisher prefix-guards under `{dir}/` (pages.go:104-109). No write/IO happens in config parsing. |
| T-07-02 | Information disclosure | config struct | accept | No secrets or PII in `DiagramsConfig`; only diagram source paths and toggles. |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | This plan installs NO packages (stdlib + existing internal types only — RESEARCH "Installation: No new dependencies"). No legitimacy gate needed. |
</threat_model>

<verification>
- `go build ./internal/releasedocs/... ./internal/config/...` succeeds.
- `make lint` reports no missing-docstring violations for `KindDiagrams`, `DiagramsConfig`, and its fields.
- `make test` (existing suite) stays green — no behavior changed, only declarations added.
</verification>

<success_criteria>
- `KindDiagrams ArtifactKind = "diagrams"` declared (DIAG-01 foundation).
- `DiagramsConfig` with five fixed per-type `[]string` lists + one inline `ArtifactConfig` gate, reachable at `cfg.Artifacts.Diagrams` (DIAG-01, D-04, D-07).
- `.cadoo.yaml.example` documents the diagrams block with the Pages-render caveat.
- Existing build, lint, and test all pass.
</success_criteria>

<output>
Create `.planning/phases/07-engineering-diagrams/07-01-SUMMARY.md` when done.
</output>
