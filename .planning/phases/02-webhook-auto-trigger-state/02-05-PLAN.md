---
phase: 02-webhook-auto-trigger-state
plan: 05
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
  - 02-03
  - 02-04
files_modified:
  - internal/riverq/queue.go
  - cmd/cadoo-webhook/main.go
  - cmd/cadoo-webhook/release.go
  - cmd/cadoo-webhook/release_test.go
autonomous: true
requirements:
  - REQ-configurable-trigger
user_setup: []

must_haves:
  truths:
    - "A GitHub release:published event builds a ReleaseJob and enqueues it"
    - "A GitHub tag push (refs/tags/v*) builds a ReleaseJob when trigger=tag and the tag matches tagPattern"
    - "A GitLab release (action=create) and tag push build a ReleaseJob"
    - "When releaseDocs.trigger excludes the event kind, no ReleaseJob is enqueued (no-op)"
    - "ReleaseArgs is a River job kind that enqueues via EnqueueRelease, parallel to ToolArgs/EnqueueTool"
  artifacts:
    - path: "internal/riverq/queue.go"
      provides: "ReleaseArgs job kind + EnqueueRelease + releaseWorker registration hook"
      contains: "ReleaseArgs"
    - path: "cmd/cadoo-webhook/release.go"
      provides: "GitHub/GitLab release + tag handlers building releasedocs.ReleaseJob"
      contains: "handleGithubRelease"
  key_links:
    - from: "cmd/cadoo-webhook/release.go"
      to: "releasedocs.ReleaseJob"
      via: "build job from event fields, strip refs/tags/ prefix, enqueue"
      pattern: "ReleaseJob"
    - from: "cmd/cadoo-webhook/main.go"
      to: "cmd/cadoo-webhook/release.go handlers"
      via: "new case arms in github/gitlab webhook type switches"
      pattern: "ReleaseEvent"
---

<objective>
Add release/tag webhook ingestion to `cadoo-webhook`: route GitHub `release:published` and tag-push events, and GitLab release (`action=create`) and tag-push events, into a `releasedocs.ReleaseJob` and enqueue it via a new dual-mode path (River `ReleaseArgs` when `DATABASE_URL` is set, in-memory queue otherwise). Honor `releaseDocs.trigger` (release vs tag) and `tagPattern` glob filtering with early no-op. Add `ReleaseArgs`/`EnqueueRelease`/`releaseWorker` to `riverq` so the worker (02-06) can consume them.

Purpose: REQ-configurable-trigger Phase 2 extension — a published release auto-triggers release-docs. This wires the dual-mode queue end-to-end (enqueue side); the consume side lands in 02-06.

Output: `riverq.ReleaseArgs` + `EnqueueRelease` + `releaseWorker` + an extended `riverq.New` that registers the release worker when a releasedocs dispatcher is supplied; webhook handlers for both providers; a second `enqueueReleaseFn` wired through `buildEnqueue`; unit tests for each handler and the trigger/tagPattern early-exit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-webhook-auto-trigger-state/02-RESEARCH.md
@.planning/phases/02-webhook-auto-trigger-state/02-02-SUMMARY.md

<interfaces>
<!-- Existing patterns to mirror exactly. -->

From internal/riverq/queue.go (ToolArgs/toolWorker/EnqueueTool — the EXACT pattern to parallel):
```go
type ToolArgs struct { Provider string; Tool string; ...; }
func (ToolArgs) Kind() string { return "tool" }
type toolWorker struct { river.WorkerDefaults[ToolArgs]; dispatcher *orchestrator.Dispatcher }
func (w *toolWorker) Work(ctx, j) error { return w.dispatcher.Run(ctx, orchestrator.ToolJob{...}) }
func New(pool *pgxpool.Pool, dispatcher *orchestrator.Dispatcher) (*Queue, error)
func (q *Queue) EnqueueTool(ctx, args ToolArgs) error
```

From internal/releasedocs/releasedocs.go:
```go
type ReleaseJob struct { Provider vcs.Kind; Repo string; Org string; FromRef string; ToRef string }
func (ReleaseJob) Kind() string { return "release_docs" }
type Dispatcher struct { ... ; func (d *Dispatcher) Run(ctx, job ReleaseJob) error }
```

From internal/config/config.go (trigger fields — already present from Phase 1):
```go
type ReleaseDocs struct {
    Enabled    bool   `yaml:"enabled"`
    Trigger    string `yaml:"trigger"`     // "release" (default) | "tag"
    TagPattern string `yaml:"tagPattern"`  // glob, empty => "v*"
    ...
}
```

From cmd/cadoo-webhook/main.go (current type switches to extend, and buildEnqueue to extend):
```go
// github switch: case *gogithub.PullRequestEvent / *gogithub.IssueCommentEvent
// gitlab switch: case *glab.MergeEvent / *glab.MergeCommentEvent
type enqueueFn func(ctx context.Context, job orchestrator.ToolJob) error
func buildEnqueue(...) (enqueueFn, func())  // returns one enqueue + cleanup
```

GitHub event fields (RESEARCH Pattern 2, VERIFIED): e.GetAction()=="published"; e.GetRelease().GetTagName(); e.GetRepo().GetFullName(); PushEvent: e.GetRef() "refs/tags/v1.2.3", e.GetCreated(), e.GetDeleted().
GitLab event fields (RESEARCH Pattern 3): *glab.ReleaseEvent{Action=="create", Tag, Project.PathWithNamespace}; *glab.TagEvent{Ref "refs/tags/...", After (zero-sha=delete), Project.PathWithNamespace}.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: riverq ReleaseArgs + EnqueueRelease + releaseWorker registration</name>
  <files>internal/riverq/queue.go</files>
  <read_first>
    - internal/riverq/queue.go (full file — ToolArgs/toolWorker/New/EnqueueTool are the exact pattern)
    - internal/releasedocs/releasedocs.go (ReleaseJob fields + Dispatcher.Run signature)
    - internal/releasedocs/dispatcher.go (Dispatcher struct — what the releaseWorker holds)
  </read_first>
  <action>
    In internal/riverq/queue.go, add a parallel release-job path mirroring the tool path:
    1. `ReleaseArgs` struct with json-tagged fields Provider, Repo, Org, FromRef, ToRef (strings). `func (ReleaseArgs) Kind() string { return "release_docs" }`.
    2. `releaseWorker` struct embedding `river.WorkerDefaults[ReleaseArgs]` and holding `dispatcher *releasedocs.Dispatcher`. Its `Work` converts ReleaseArgs → releasedocs.ReleaseJob (Provider via vcs.Kind(...)) and calls dispatcher.Run.
    3. Change `New` to also accept a release dispatcher so the worker process can register releaseWorker. Use a functional-options OR a second parameter — prefer adding a parameter `releaseDispatcher *releasedocs.Dispatcher` (update the existing two call sites in cmd/cadoo-webhook/main.go and cmd/cadoo-worker/main.go to pass nil for now; 02-06 wires the worker's real dispatcher). Inside New, when releaseDispatcher != nil register `river.AddWorker(workers, &releaseWorker{dispatcher: releaseDispatcher})` alongside the existing toolWorker registration (Pitfall 4: both workers must be registered before NewClient).
    4. `func (q *Queue) EnqueueRelease(ctx context.Context, args ReleaseArgs) error` mirroring EnqueueTool (`q.client.Insert(ctx, args, nil)`).

    riverq already imports orchestrator (it is the composition tier, allowed — D-01 forbids releasedocs→orchestrator/riverq, NOT riverq→releasedocs). Import internal/releasedocs here. Docstring every new exported symbol.
  </action>
  <verify>
    <automated>go build ./internal/riverq/... && go vet ./internal/riverq/...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "func (ReleaseArgs) Kind() string" internal/riverq/queue.go` returns 1 and returns "release_docs"
    - `grep -c "func (q \*Queue) EnqueueRelease" internal/riverq/queue.go` returns 1
    - `grep -c "river.AddWorker(workers, &releaseWorker" internal/riverq/queue.go` returns 1
    - `go build ./...` exits 0 (both riverq.New call sites updated to the new signature)
  </acceptance_criteria>
  <done>riverq has ReleaseArgs/EnqueueRelease/releaseWorker; New registers the release worker when given a releasedocs dispatcher; module builds with updated call sites.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Webhook release/tag handlers + trigger/tagPattern early-exit</name>
  <files>cmd/cadoo-webhook/release.go, cmd/cadoo-webhook/release_test.go</files>
  <read_first>
    - cmd/cadoo-webhook/main.go (handleGithubPR/handleGithubComment/handleGitlabMR shapes; buildEnqueue; the enqueueFn typedef)
    - .planning/phases/02-webhook-auto-trigger-state/02-RESEARCH.md (Patterns 2-4 + Pitfall 1 tag-prefix + Pitfall 2 async-no-op guidance)
    - internal/config/config.go (ReleaseDocs.Trigger / TagPattern semantics)
    - internal/releasedocs/releasedocs.go (ReleaseJob fields)
  </read_first>
  <behavior>
    - GitHub release published: e.Action=="published" → handler builds ReleaseJob{Provider: KindGitHub, Repo: full name, ToRef: tag} and calls enqueueRelease once. Non-"published" actions (draft, edited, deleted) → no enqueue.
    - GitHub tag push: ref="refs/tags/v1.2.3", created=true, deleted=false, trigger=="tag", tag matches tagPattern → enqueue with ToRef="v1.2.3" (prefix stripped). Tag deletion (deleted=true) → no enqueue. Tag not matching tagPattern (e.g. "v1.2.3-rc.1" with pattern "v*" — note path.Match("v*","v1.2.3-rc.1") matches; use the documented default and assert the rc-exclusion case only if pattern excludes it) → follow path.Match result.
    - Trigger early-exit: trigger=="tag" but a release event arrives → no enqueue. trigger=="release" (or empty) but a tag-push event arrives → no enqueue.
    - GitLab release: Action=="create" → enqueue ReleaseJob{Provider: KindGitLab, Repo: Project.PathWithNamespace, ToRef: Tag}. Other actions → no enqueue.
    - GitLab tag push: Ref="refs/tags/v1.2.3", After non-zero → enqueue ToRef="v1.2.3"; After zero-sha → no enqueue.
  </behavior>
  <action>
    Create cmd/cadoo-webhook/release.go. Add an `enqueueReleaseFn func(ctx context.Context, job releasedocs.ReleaseJob) error` type. Implement `handleGithubRelease`, `handleGithubTagPush`, `handleGitlabRelease`, `handleGitlabTagPush`.

    Per RESEARCH Pitfall 2, do NOT load .cadoo.yaml synchronously in the HTTP handler for the trigger check — that blocks the request goroutine on a VCS API call. Instead: the webhook always enqueues when the EVENT KIND structurally matches a release-docs trigger, and the dispatcher (worker) performs the authoritative `releaseDocs.enabled`/config check. BUT the trigger vs tag distinction and tagPattern glob are cheap and payload-local, so apply them in the handler from a webhook-side default/config source: since per-PR config is not loaded here, gate release-vs-tag using a package-level default of "release" unless overridden — OR (preferred, matches SPEC "no-ops early") accept a `releaseTrigger`/`tagPattern` resolved from settings/env if present, defaulting trigger="release", tagPattern="v*". Implement the structural rules: release events enqueue only when trigger allows release (default yes); tag-push events enqueue only when trigger=="tag" AND path.Match(tagPattern, tagName) is true AND the push is a creation (created && !deleted for GitHub; After != zero-sha for GitLab). Always strip "refs/tags/" via strings.TrimPrefix (Pitfall 1).

    Build the ReleaseJob with Provider, Repo, ToRef (leave FromRef empty — the dispatcher resolves it via LatestTagBefore). Log enqueue at info, errors at error, skips at debug with the reason.
  </action>
  <verify>
    <automated>go test -race -count=1 ./cmd/cadoo-webhook/...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "strings.TrimPrefix(.*\"refs/tags/\")" cmd/cadoo-webhook/release.go` returns at least 2 (GitHub + GitLab tag handlers)
    - `grep -c "path.Match" cmd/cadoo-webhook/release.go` returns at least 1 (tagPattern filtering)
    - Tests exist named TestHandleGithubRelease, TestHandleGithubTagPush, TestHandleGitlabRelease, TestHandleGitlabTagPush, and a trigger/tag early-exit test, each asserting whether a ReleaseJob was enqueued via a recording enqueueReleaseFn
    - The early-exit test asserts zero enqueues when the event kind does not match the trigger
    - `go test -race -count=1 ./cmd/cadoo-webhook/...` passes
  </acceptance_criteria>
  <done>Release/tag handlers for both providers build correct ReleaseJobs with stripped tags and tagPattern filtering; trigger mismatch yields no enqueue; tests cover every handler and the early-exit.</done>
</task>

<task type="auto">
  <name>Task 3: Wire handlers into webhook switches + dual-mode release enqueue</name>
  <files>cmd/cadoo-webhook/main.go</files>
  <read_first>
    - cmd/cadoo-webhook/main.go (githubWebhookHandler/gitlabWebhookHandler type switches; buildEnqueue River + in-memory branches; jobs.NewMemory().Register pattern)
    - cmd/cadoo-webhook/release.go (the handlers + enqueueReleaseFn type from Task 2)
    - internal/riverq/queue.go (EnqueueRelease + ReleaseArgs from Task 1)
    - internal/jobs/jobs.go (Queue.Register(kind, Handler) — how release_docs would register in memory mode)
  </read_first>
  <action>
    1. Add case arms to the GitHub switch: `case *gogithub.ReleaseEvent:` → handleGithubRelease(...); `case *gogithub.PushEvent:` → handleGithubTagPush(...). Add to the GitLab switch: `case *glab.ReleaseEvent:` → handleGitlabRelease(...); `case *glab.TagEvent:` → handleGitlabTagPush(...). Pass the new enqueueReleaseFn into each handler.
    2. Extend buildEnqueue (or add buildReleaseEnqueue reusing the same pool/queue — Pitfall 3) to ALSO return an `enqueueReleaseFn`. River branch: `func(ctx, job releasedocs.ReleaseJob) error { return rq.EnqueueRelease(ctx, riverq.ReleaseArgs{Provider: string(job.Provider), Repo: job.Repo, Org: job.Org, FromRef: job.FromRef, ToRef: job.ToRef}) }`. In-memory branch: register the release_docs kind on the same memory queue with a handler that runs the releasedocs dispatcher — for the webhook's in-memory single-binary mode, build a releasedocs.Dispatcher (VCSPool from the existing buildDispatcher's pool, defaults.DefaultGenerators()+blog, defaults.DefaultPublishers()+pages) and register it; if no releasedocs dispatcher can be built (no VCS), enqueueRelease becomes a logged no-op. Keep the existing toolWorker enqueue path unchanged.
    3. Update the riverq.New call in buildEnqueue to pass nil for the release dispatcher (webhook process only enqueues; the worker registers the consumer). Update main() wiring so both enqueue and enqueueRelease (and their cleanups) are threaded to the handlers.

    Do not merge the two enqueue function signatures into one interface{} (Pitfall 3 — keep both typed). The webhook always returns 202 Accepted regardless of trigger outcome (Pitfall 2).
  </action>
  <verify>
    <automated>go build ./cmd/cadoo-webhook/... && go test -race -count=1 ./cmd/cadoo-webhook/...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "case \*gogithub.ReleaseEvent" cmd/cadoo-webhook/main.go` returns 1
    - `grep -c "case \*gogithub.PushEvent" cmd/cadoo-webhook/main.go` returns 1
    - `grep -c "case \*glab.ReleaseEvent" cmd/cadoo-webhook/main.go` returns 1
    - `grep -c "case \*glab.TagEvent" cmd/cadoo-webhook/main.go` returns 1
    - `grep -c "EnqueueRelease" cmd/cadoo-webhook/main.go` returns at least 1 (River branch)
    - `go build ./cmd/cadoo-webhook/...` exits 0 and `go test -race ./cmd/cadoo-webhook/...` passes
  </acceptance_criteria>
  <done>Both webhook switches route release/tag events to the new handlers; dual-mode enqueueRelease wired (River EnqueueRelease + in-memory dispatcher registration); webhook builds and tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inbound webhook → enqueue | untrusted release/tag payload crosses here; signature already verified upstream by VerifySignature/VerifyToken |
| tag name → ReleaseJob.ToRef | payload tag name flows into a job consumed downstream (paths, DB) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Spoofing | release/tag webhook handlers | mitigate | Handlers run only after the existing HMAC-SHA256 (GitHub) / constant-time token (GitLab) verification in the webhook handler; no new auth surface |
| T-02-10 | Tampering (path traversal seed) | tag name → ReleaseJob.ToRef | mitigate | tagPattern path.Match gate on tag-push; "refs/tags/" stripped via strings.TrimPrefix; downstream pages publisher uses path.Join (02-04) |
| T-02-11 | Denial of Service | enqueue storm on re-tagging | mitigate | Webhook returns 202 immediately; River unique-on-(to_tag) state and the 02-02 unique constraint collapse duplicate in-flight work; dispatcher no-ops disabled configs |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs; go-github v66 + go-gitlab v1.46 already in go.mod (RESEARCH.md: zero new dependencies) |
</threat_model>

<verification>
- `go build ./...` green (riverq.New signature change propagated to both call sites)
- `go test -race -count=1 ./cmd/cadoo-webhook/... ./internal/riverq/...` green
- Trigger early-exit proven: trigger-excluded events enqueue zero jobs
- `make lint` clean
</verification>

<success_criteria>
- GitHub release:published + tag push and GitLab release + tag push build ReleaseJobs and enqueue (dual-mode)
- releaseDocs.trigger mismatch and tagPattern non-match result in no enqueue
- riverq.ReleaseArgs/EnqueueRelease exist and the release worker registers when a releasedocs dispatcher is supplied
</success_criteria>

<output>
Create `.planning/phases/02-webhook-auto-trigger-state/02-05-SUMMARY.md` when done
</output>
