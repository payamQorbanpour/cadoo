---
phase: 03-api-docs-openapi
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: false
requirements: [REQ-release-artifact-generation]
files_modified:
  - go.mod
  - go.sum
  - internal/releasedocs/releasedocs.go
  - internal/releasedocs/dispatcher.go
  - internal/releasedocs/publishers/pages/pages.go
  - internal/config/config.go
  - internal/releasedocs/generators/apidocs/assets/redoc.standalone.js
  - internal/releasedocs/generators/apidocs/.redoc-version
user_setup: []

must_haves:
  truths:
    - "libopenapi + libopenapi-validator are in go.mod and the module builds"
    - "Redoc v2.5.3 standalone bundle is vendored under apidocs/assets and committed"
    - "Artifact carries an optional Filename that pages publisher honors for non-.md extensions"
    - "A generator can emit multiple artifacts via the MultiGenerator interface and the dispatcher spreads them"
    - "Existing changelog/releasenotes/blog .md paths and golden tests are unchanged"
  artifacts:
    - path: "internal/releasedocs/releasedocs.go"
      provides: "Artifact.Filename field, KindAPIDocs constant, MultiGenerator interface"
      contains: "MultiGenerator"
    - path: "internal/releasedocs/dispatcher.go"
      provides: "MultiGenerator type-assertion that spreads []Artifact"
      contains: "GenerateMulti"
    - path: "internal/releasedocs/publishers/pages/pages.go"
      provides: "Filename-aware path construction with traversal guard preserved"
      contains: "art.Filename"
    - path: "internal/config/config.go"
      provides: "APIDocsConfig with SpecPath; ReleaseArtifacts.APIDocs field"
      contains: "APIDocsConfig"
    - path: "internal/releasedocs/generators/apidocs/assets/redoc.standalone.js"
      provides: "vendored Redoc bundle (go:embed target)"
      min_lines: 1
  key_links:
    - from: "internal/releasedocs/dispatcher.go"
      to: "MultiGenerator.GenerateMulti"
      via: "type assertion gen.(MultiGenerator)"
      pattern: "\\.\\(.*MultiGenerator\\)"
    - from: "internal/releasedocs/publishers/pages/pages.go"
      to: "art.Filename"
      via: "filename override with fallback to string(art.Kind)+\".md\""
      pattern: "art\\.Filename"
---

<objective>
Lay the Wave 0 foundation for the apidocs generator family: add the two libopenapi dependencies, vendor the Redoc bundle, and make the four cross-cutting model/wiring changes that everything else in this phase compiles against — the `Artifact.Filename` field, the `KindAPIDocs` constant, the `MultiGenerator` multi-artifact interface + dispatcher spread, the pages-publisher filename change, and the `APIDocsConfig` config block.

Purpose: No apidocs code (parse/render/generator) can compile until these contracts and dependencies exist. This is the [BLOCKING] plan — Plans 02-05 all depend on it.
Output: Updated go.mod/go.sum, vendored Redoc asset, extended `releasedocs.go`/`dispatcher.go`/`pages.go`/`config.go`. Existing Phase 1/2 golden + idempotency tests stay green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-api-docs-openapi/03-CONTEXT.md
@.planning/phases/03-api-docs-openapi/03-RESEARCH.md
@.planning/phases/03-api-docs-openapi/03-PATTERNS.md

<interfaces>
<!-- Current contracts the executor extends. From internal/releasedocs/releasedocs.go -->

Artifact (releasedocs.go:62-69) — currently { Kind ArtifactKind; Content []byte }. Add a Filename string field (zero-value "" = backward compatible).

ArtifactKind constants (releasedocs.go:20-29) — KindChangelog, KindReleaseNotes, KindBlog. Add KindAPIDocs = "apidocs".

Generator interface (releasedocs.go:145-155) — Kind() ArtifactKind; Enabled(cfg, bump) bool; Generate(ctx, rc) (Artifact, error). DO NOT change this signature (blog/changelog/releasenotes implement it and have golden tests).

Dispatcher generate loop (dispatcher.go:104-115) — the ONLY caller of gen.Generate in the subsystem. Iterates d.Generators, calls Enabled then Generate, appends one Artifact to arts.

pages.Publisher.Publish path formula (pages.go:81) — p := path.Join(dir, "releases", rc.ToRef, string(art.Kind)+".md"). The expectedPrefix traversal guard is at pages.go:85-90.

config.ReleaseArtifacts (config.go:75-85) — Changelog, ReleaseNotes, Blog. ReleaseNotesConfig (config.go:107-112) embeds ArtifactConfig inline + adds a field; mirror that pattern for APIDocsConfig.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify package legitimacy before installing (T-03-SC)</name>
  <read_first>.planning/phases/03-api-docs-openapi/03-RESEARCH.md (§ Package Legitimacy Audit — both pb33f packages tagged [ASSUMED])</read_first>
  <what-built>Nothing yet — this gate precedes the `go get` install task. slopcheck was unavailable at research time, so both pb33f modules are tagged [ASSUMED] and require human verification before install.</what-built>
  <how-to-verify>
    1. Open https://pkg.go.dev/github.com/pb33f/libopenapi — confirm it exists, is MIT-licensed, source repo github.com/pb33f/libopenapi, actively maintained.
    2. Open https://pkg.go.dev/github.com/pb33f/libopenapi-validator — confirm it exists, MIT, same author (pb33f).
    3. Open https://github.com/Redocly/redoc/blob/main/LICENSE — confirm MIT for the Redoc bundle to be vendored.
    Confirm none look typosquatted or abandoned.
  </how-to-verify>
  <acceptance_criteria>Human confirms all three packages are legitimate and MIT-licensed. This checkpoint is NEVER auto-approvable.</acceptance_criteria>
  <resume-signal>Type "approved" to proceed with installs, or describe concerns.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Add libopenapi deps and vendor the Redoc bundle</name>
  <files>go.mod, go.sum, internal/releasedocs/generators/apidocs/assets/redoc.standalone.js, internal/releasedocs/generators/apidocs/.redoc-version</files>
  <read_first>.planning/phases/03-api-docs-openapi/03-RESEARCH.md (§ Standard Stack — install commands, versions; § Environment Availability — npm fallback; Open Question 3 — bundle vendoring path)</read_first>
  <action>Run `go get github.com/pb33f/libopenapi@v0.37.3` and `go get github.com/pb33f/libopenapi-validator@latest`, then `go mod tidy`. Vendor the Redoc v2.5.3 standalone bundle to `internal/releasedocs/generators/apidocs/assets/redoc.standalone.js` — either `npm install redoc@2.5.3 && cp node_modules/redoc/bundles/redoc.standalone.js <dest>`, or if npm is unavailable download from `https://github.com/Redocly/redoc/releases/download/v2.5.3/redoc.standalone.js`. Write a sidecar `.redoc-version` file recording the pinned version (2.5.3) and the sha256 of the bundle for provenance. Note in the commit message that the embedded bundle adds ~1.2 MB uncompressed to each binary (RESEARCH Pitfall 4 — this is accepted; servers, not one-shot CLI). Do NOT reference the bundle via any CDN.</action>
  <verify>
    <automated>go build ./... && go list -m github.com/pb33f/libopenapi github.com/pb33f/libopenapi-validator && test -s internal/releasedocs/generators/apidocs/assets/redoc.standalone.js</automated>
  </verify>
  <acceptance_criteria>`go list -m` prints both modules with resolved versions; `assets/redoc.standalone.js` exists and is non-empty; `.redoc-version` records version + sha256; `go build ./...` succeeds; no `go.mod` replace directives were introduced by `go mod tidy` (RESEARCH Pitfall 6).</acceptance_criteria>
  <done>Both deps in go.mod, Redoc bundle vendored + provenance recorded, module builds.</done>
</task>

<task type="auto">
  <name>Task 3: Extend Artifact + KindAPIDocs + MultiGenerator interface + dispatcher spread</name>
  <files>internal/releasedocs/releasedocs.go, internal/releasedocs/dispatcher.go</files>
  <read_first>internal/releasedocs/releasedocs.go (Artifact struct lines 62-69; ArtifactKind constants 20-29; Generator interface 145-155); internal/releasedocs/dispatcher.go (generate loop lines 104-115); .planning/phases/03-api-docs-openapi/03-PATTERNS.md (Critical Planning Notes 1-2; releasedocs.go model section)</read_first>
  <action>In `releasedocs.go`: (a) add an optional `Filename string` field to `Artifact` with a docstring stating non-empty overrides the default "{kind}.md" path and empty preserves backward compat. (b) add a `KindAPIDocs ArtifactKind = "apidocs"` constant with docstring noting one Generate emits three Filename-differentiated artifacts. (c) add a NEW optional interface `MultiGenerator` with method `GenerateMulti(ctx context.Context, rc ReleaseContext) ([]Artifact, error)` and a docstring explaining the dispatcher prefers it when a generator implements it (for families emitting multiple files); leave the base `Generator` interface signature UNCHANGED. In `dispatcher.go` generate loop (lines 104-115): after `Enabled`, type-assert `gen.(MultiGenerator)` — when present call `GenerateMulti` and append all returned artifacts via `arts = append(arts, multi...)`; otherwise keep the existing single-`Generate` path. Preserve the existing error-wrap `fmt.Errorf("releasedocs: generator %s: ...", gen.Kind(), err)`. Every new exported symbol needs a docstring (revive `exported` is ON). No fenced code in the file beyond Go source.</action>
  <verify>
    <automated>go build ./... && go vet ./internal/releasedocs/... && grep -q "GenerateMulti" internal/releasedocs/dispatcher.go && grep -q "Filename string" internal/releasedocs/releasedocs.go</automated>
  </verify>
  <acceptance_criteria>`releasedocs.go` declares `Artifact.Filename`, `KindAPIDocs`, and `MultiGenerator`; the base `Generator` interface signature is byte-identical to before (blog/changelog/releasenotes still satisfy it); `dispatcher.go` type-asserts `MultiGenerator` and spreads `[]Artifact`; package builds and vets clean.</acceptance_criteria>
  <done>Multi-artifact contract exists; dispatcher spreads multi-generator output; existing generators untouched.</done>
</task>

<task type="auto">
  <name>Task 4: Pages publisher honors Artifact.Filename + APIDocsConfig in config</name>
  <files>internal/releasedocs/publishers/pages/pages.go, internal/config/config.go</files>
  <read_first>internal/releasedocs/publishers/pages/pages.go (Publish loop lines 74-100; path formula line 81; traversal guard 85-90); internal/config/config.go (ReleaseArtifacts 75-85; ArtifactConfig 87-103; ReleaseNotesConfig inline-embed 107-112); .planning/phases/03-api-docs-openapi/03-PATTERNS.md (pages.go + config.go sections)</read_first>
  <action>In `pages.go`: replace the single hardcoded line 81 with a 3-line guard — set `filename := art.Filename`; if `filename == ""` set `filename = string(art.Kind) + ".md"`; then `p := path.Join(dir, "releases", rc.ToRef, filename)`. Leave the `expectedPrefix` traversal guard (lines 85-90) UNCHANGED so a `Filename` carrying `../` is still rejected (T-03 Tampering mitigation). Update the package/Publish doc comments that say "{kind}.md" to note Filename override. In `config.go`: add an `APIDocs APIDocsConfig` field (yaml:"apiDocs") to `ReleaseArtifacts`, and define a new `APIDocsConfig` struct embedding `ArtifactConfig` inline (yaml:",inline") plus `SpecPath string` (yaml:"specPath") — mirror `ReleaseNotesConfig` exactly. Docstring `APIDocsConfig` and `SpecPath` noting SpecPath default "" → fallback discovery list (D-02), and that enabled+when gate all three outputs as a family (D-07). Run goimports (cadoo-internal third group).</action>
  <verify>
    <automated>go build ./... && go test -race ./internal/releasedocs/publishers/pages/... && go test ./internal/config/...</automated>
  </verify>
  <acceptance_criteria>pages.go uses `art.Filename` when non-empty and falls back to `string(art.Kind)+".md"` when empty; the `expectedPrefix` guard is still present and applies to the computed path; existing `internal/releasedocs/publishers/pages` test suite (including TestDeterministicPaths backward-compat) stays green; `config.APIDocsConfig` exists with embedded `ArtifactConfig` + `SpecPath`; `ReleaseArtifacts.APIDocs` field present.</acceptance_criteria>
  <done>Pages publisher routes per-Filename extension with traversal guard intact; config block for apidocs exists; no existing test changed its expectations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generator-produced Artifact → pages publisher path | A generator could set a `Filename` containing `../`; the computed commit path must not escape `{dir}/`. |
| Vendored third-party JS bundle → embedded in binary | The Redoc bundle becomes part of the shipped binary; provenance must be pinned and verifiable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | pages.go path from `art.Filename` | mitigate | Keep the existing `expectedPrefix` (`dir + "/"`) `strings.HasPrefix` guard unchanged; it runs on the computed path whether Filename is set or not, rejecting `../` escapes (Task 4). |
| T-03-02 | Tampering | vendored redoc.standalone.js | mitigate | Pin Redoc v2.5.3, record sha256 in `.redoc-version`, verify MIT license at the human checkpoint (Task 1) before vendoring (Task 2). |
| T-03-SC | Tampering | `go get` of pb33f/libopenapi + libopenapi-validator | mitigate | slopcheck + blocking human checkpoint (Task 1) verifying both [ASSUMED] packages on pkg.go.dev before install. Never auto-approvable. |
</threat_model>

<verification>
- `go build ./...` succeeds with both new deps and the embedded asset present.
- `make test` (or at minimum `go test -race ./internal/releasedocs/... ./internal/config/...`) is green — no Phase 1/2 golden or idempotency regression.
- `grep` confirms `Filename`, `KindAPIDocs`, `MultiGenerator`, `GenerateMulti`, `APIDocsConfig` are present.
</verification>

<success_criteria>
- Both libopenapi modules resolved in go.mod; module builds.
- Redoc v2.5.3 bundle vendored with sha256 provenance; no CDN reference anywhere.
- `Artifact.Filename` + `KindAPIDocs` + `MultiGenerator` exist; dispatcher spreads multi-artifact output.
- Pages publisher honors Filename with the traversal guard intact; existing tests green.
- `config.APIDocsConfig` block exists.
</success_criteria>

<output>
Create `.planning/phases/03-api-docs-openapi/03-01-SUMMARY.md` when done.
</output>
