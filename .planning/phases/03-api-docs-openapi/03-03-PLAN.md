---
phase: 03-api-docs-openapi
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
autonomous: true
requirements: [REQ-release-artifact-generation]
files_modified:
  - internal/releasedocs/generators/apidocs/discover.go
  - internal/releasedocs/generators/apidocs/parse.go
user_setup: []

must_haves:
  truths:
    - "Spec discovery fetches apiDocs.specPath exactly when set, else tries the ordered fallback list and uses the first hit (D-02)"
    - "Non-404 fetch errors are logged and skipped rather than aborting discovery"
    - "Specs parse via libopenapi with remote AND file $ref resolution disabled"
    - "Specs over 5 MB are rejected before parsing (OOM guard)"
    - "Supported set is defined by spec version, not web framework — Swagger 2.0 + OAS 3.0.x + 3.1.x (D-03, D-09)"
    - "OAS 3.x is schema-validated via libopenapi-validator; Swagger 2.0 is parse-only behind an isolated function with a deprecation TODO"
    - "Unsupported versions and any parse/validation failure return a reasoned error so the generator skips"
  artifacts:
    - path: "internal/releasedocs/generators/apidocs/discover.go"
      provides: "discoverSpec fallback loop + isMissingFile 404 tolerance"
      contains: "FetchFileFromRef"
    - path: "internal/releasedocs/generators/apidocs/parse.go"
      provides: "loadSpec: NewDocumentWithConfiguration, version detect, validate, sorted path extraction; max-size guard"
      contains: "AllowRemoteReferences"
  key_links:
    - from: "internal/releasedocs/generators/apidocs/parse.go"
      to: "libopenapi.NewDocumentWithConfiguration"
      via: "datamodel.DocumentConfiguration{AllowRemoteReferences:false, AllowFileReferences:false}"
      pattern: "AllowRemoteReferences"
    - from: "internal/releasedocs/generators/apidocs/discover.go"
      to: "releasedocs.FileFetcher"
      via: "type-assert rc.Provider then FetchFileFromRef at rc.ToRef"
      pattern: "FetchFileFromRef"
---

<objective>
Implement the spec ingestion layer: `discover.go` (locate + fetch the committed spec) and `parse.go` (parse with libopenapi, detect version, validate, extract a sorted path model) — including the SSRF ($ref) and OOM (max-size) security guards. These are the deterministic inputs the renderers (Plan 04) and the generator (Plan 05) consume.

Purpose: All untrusted-spec handling lives here. Every failure mode returns a reasoned error so the generator can skip-with-log (D-10).
Output: `discover.go`, `parse.go`. Turns the discovery/parse/validation/SSRF tests from Plan 02 green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-api-docs-openapi/03-RESEARCH.md
@.planning/phases/03-api-docs-openapi/03-PATTERNS.md

<interfaces>
<!-- From 03-01: config.APIDocsConfig{ArtifactConfig inline, SpecPath}; releasedocs.FileFetcher -->
<!-- From internal/releasedocs/template/template.go: isMissingFile (lines 132-142) + FileFetcher type-assert (151-167) -->

releasedocs.FileFetcher.FetchFileFromRef(ctx, repo, ref, path) ([]byte, error) — missing file = error; apply isMissingFile (errors.Is fs.ErrNotExist OR msg contains "404"/"not found").

config access: rc.Config.Artifacts.APIDocs.SpecPath (string), rc.Repo, rc.ToRef.

libopenapi API (RESEARCH Pattern 1 + Code Examples): libopenapi.NewDocumentWithConfiguration(bytes, *datamodel.DocumentConfiguration); doc.GetSpecInfo().SpecType (use the named constants from github.com/pb33f/libopenapi/utils — verify utils.OpenApi2/OpenApi3 values at impl time, Open Question 1); doc.BuildV3Model() / doc.BuildV2Model(); validator.NewValidator(doc).ValidateDocument() (OAS 3.x ONLY); v3Model.Model.Paths.PathItems.KeysFromOldest() ordered iterator.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: discover.go — fallback spec discovery with 404 tolerance</name>
  <files>internal/releasedocs/generators/apidocs/discover.go</files>
  <read_first>internal/releasedocs/template/template.go (isMissingFile lines 132-142; FileFetcher type-assert 151-167); .planning/phases/03-api-docs-openapi/03-PATTERNS.md (discover.go section — fallbackPaths list, discoverSpec); .planning/phases/03-api-docs-openapi/03-RESEARCH.md (Pitfall 5 — 404 tolerance vs auth error)</read_first>
  <action>Create `package apidocs`. Copy the `isMissingFile(err error) bool` helper verbatim from template.go (errors.Is fs.ErrNotExist OR msg contains "404"/"not found") as a package-private function. Define `var fallbackPaths = []string{"openapi.yaml", "openapi.yml", "openapi.json", "docs/openapi.yaml", "api/openapi.yaml"}` (D-02 order). Implement `discoverSpec(ctx context.Context, rc releasedocs.ReleaseContext) ([]byte, error)`: type-assert `rc.Provider.(releasedocs.FileFetcher)` — if absent return an error "provider does not implement FileFetcher". Read `specPath := rc.Config.Artifacts.APIDocs.SpecPath`; if non-empty, fetch EXACTLY that path at `rc.ToRef` (no fallback per D-02) and return its result/error. If empty, loop `fallbackPaths`: on success return bytes; on `isMissingFile(err)` continue to next; on any other (non-404) error log via slog.Warn (path + err) and continue (Pitfall 5 — never abort discovery on a transient auth error). After the loop return an error "no spec found at any fallback path". Every exported symbol (none here are exported, but the file-level package doc may suffice) — keep functions package-private. goimports third group = cadoo-internal.</action>
  <verify>
    <automated>go test -race -run TestDiscoverSpec ./internal/releasedocs/generators/apidocs/...</automated>
  </verify>
  <acceptance_criteria>`TestDiscoverSpec` passes: specPath set → exactly-one fetch of that path at ToRef, no fallback; specPath empty → fallback in the D-02 order, first hit wins; all-404 → error; non-404 error logged + skipped not aborted. `isMissingFile` present.</acceptance_criteria>
  <done>Spec discovery resolves the committed spec or returns a reasoned not-found error.</done>
</task>

<task type="auto">
  <name>Task 2: parse.go — libopenapi parse + version detect + validation + security guards</name>
  <files>internal/releasedocs/generators/apidocs/parse.go</files>
  <read_first>.planning/phases/03-api-docs-openapi/03-RESEARCH.md (Pattern 1; Code Examples loadSpec; § Security Domain — $ref SSRF + max-size; Pitfall 1 — v2 deprecation isolation; Open Question 1 — SpecType constants); .planning/phases/03-api-docs-openapi/03-PATTERNS.md (parse.go section — NewDocumentWithConfiguration, maxSpecSize guard)</read_first>
  <action>Create the parse layer in `package apidocs`. Define `const maxSpecSize = 5 * 1024 * 1024` and a `specModel` (or `specInfo`) struct holding version string, title, and an extracted ordered path model (a slice of operation/parameter structs shared with the Markdown renderer — coordinate field names with Plan 04's APIDocsData; expose enough for both renderers). Implement `loadSpec(specBytes []byte) (*specModel, error)`: (1) FIRST guard `if len(specBytes) > maxSpecSize` return an error "spec too large (N bytes, limit 5242880)" — BEFORE any parsing (OOM/DoS mitigation). (2) Build `cfg := datamodel.DocumentConfiguration{AllowRemoteReferences: false, AllowFileReferences: false}` and call `libopenapi.NewDocumentWithConfiguration(specBytes, &cfg)` — NOT plain NewDocument (SSRF mitigation — remote/file $ref must not resolve). (3) `info := doc.GetSpecInfo()`; switch on `info.SpecType` using the named `utils.OpenApi2`/`utils.OpenApi3` constants (import github.com/pb33f/libopenapi/utils; verify exact values at impl time per Open Question 1): for OAS 3.x → `doc.BuildV3Model()` (return error on build errs), then `validator.NewValidator(doc).ValidateDocument()` — on `!valid` return an error with the first validation message (D-10 validation-failure skip); then extract paths via `v3Model.Model.Paths.PathItems.KeysFromOldest()`, collect keys, `sort.Strings` them for golden determinism, and build the ordered path model. For Swagger 2.0 → isolate ALL v2 handling in a single package-private `parseSwagger2(doc)` function with a prominent `// TODO: libopenapi v2 model is deprecated and will be removed; revisit when libopenapi drops it.` comment; emit `slog.Warn("apidocs: Swagger 2.0 spec; v2 model deprecated; limited validation", "version", info.Version)`; use `doc.BuildV2Model()` for parse + path iteration ONLY (no schema validation — libopenapi-validator is OAS 3.x-only); sort v2 paths for determinism. (4) default branch → return an error `unsupported spec version %q (want swagger 2.0, openapi 3.x)` (D-10 unsupported skip). Every exported symbol needs a docstring; keep the model types exported only if Plan 04 imports them within the same package (same package → unexported is fine). Do not introduce a second YAML library — let libopenapi-validator use its own (Pitfall 6).</action>
  <verify>
    <automated>go test -race -run 'TestGenerate_OAS3|TestGenerate_OAS31|TestGenerate_Swagger2|TestGenerate_ParseFailure_Skips|TestGenerate_ValidationFailure_Skips|TestGenerate_UnsupportedVersion_Skips|TestGenerate_NoRemoteRef' ./internal/releasedocs/generators/apidocs/... 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>`loadSpec` rejects >5 MB specs before parsing; uses `NewDocumentWithConfiguration` with `AllowRemoteReferences:false` AND `AllowFileReferences:false` (TestGenerate_NoRemoteRef green — remote/file $ref not resolved); OAS 3.x validated via libopenapi-validator (validation-failure → error); Swagger 2.0 isolated in `parseSwagger2` with deprecation TODO + warn log, parse-only; unknown version → error; path extraction is sorted-deterministic. The named version tests pass.</acceptance_criteria>
  <done>Spec parsing/validation with SSRF + OOM guards complete; all version + skip-reason paths return reasoned errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VCS spec bytes → libopenapi parser | The committed spec is untrusted input authored by the repo owner; it may contain hostile `$ref`s or be oversized. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Information Disclosure | `$ref` resolution in libopenapi | mitigate | parse.go MUST use `NewDocumentWithConfiguration` with `AllowRemoteReferences:false` AND `AllowFileReferences:false`; `TestGenerate_NoRemoteRef` asserts a `file:///`/remote `$ref` is not resolved (Task 2). |
| T-03-04 | Denial of Service | oversized spec → OOM | mitigate | `maxSpecSize = 5 MB` length check runs BEFORE `NewDocumentWithConfiguration`; oversized → reasoned error → generator skips (Task 2). |
| T-03-05 | Tampering | Swagger 2.0 deprecated v2 model | accept (isolated) | All v2 calls isolated in `parseSwagger2` with a deprecation TODO + slog.Warn; parse-success treated as "valid enough" (no maintained v2 validator exists; RESEARCH Pitfall 1). |
| T-03-06 | Spoofing/Validation | malformed/invalid spec published as docs | mitigate | Parse failure and OAS 3.x validation failure both return reasoned errors → generator skips → never publishes garbage docs (D-10, Task 2). |
</threat_model>

<verification>
- `go test -race -run TestDiscoverSpec ./internal/releasedocs/generators/apidocs/...` green.
- `go test -race -run 'TestGenerate_OAS3|TestGenerate_OAS31|TestGenerate_Swagger2|...skip tests...|TestGenerate_NoRemoteRef' ./...` green.
- `grep AllowRemoteReferences internal/releasedocs/generators/apidocs/parse.go` matches; `grep -q maxSpecSize parse.go`.
</verification>

<success_criteria>
- Discovery honors specPath/fallback per D-02 with 404 tolerance.
- Parse uses libopenapi with remote+file refs disabled and a 5 MB pre-parse guard.
- OAS 3.x validated; Swagger 2.0 isolated + parse-only; unsupported/parse/validation failures all return reasoned errors.
</success_criteria>

<output>
Create `.planning/phases/03-api-docs-openapi/03-03-SUMMARY.md` when done.
</output>
