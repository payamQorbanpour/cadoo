---
phase: 03-api-docs-openapi
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
autonomous: true
requirements: [REQ-release-artifact-generation, REQ-release-docs-idempotency]
files_modified:
  - internal/releasedocs/generators/apidocs/apidocs_test.go
  - internal/releasedocs/generators/apidocs/testdata/petstore_v2.yaml
  - internal/releasedocs/generators/apidocs/testdata/petstore_v3.yaml
  - internal/releasedocs/generators/apidocs/testdata/petstore_v31.yaml
  - internal/releasedocs/generators/apidocs/testdata/invalid.yaml
  - internal/releasedocs/generators/apidocs/testdata/remote_ref.yaml
  - internal/releasedocs/generators/apidocs/testdata/oversized.yaml
  - internal/releasedocs/publishers/pages/pages_apidocs_test.go
user_setup: []

must_haves:
  truths:
    - "Test fixtures exist for Swagger 2.0, OAS 3.0, OAS 3.1, malformed, remote-$ref, and oversized specs"
    - "A reusable fakeFetcher implementing FileFetcher + vcs.Provider drives table-driven tests without a real VCS"
    - "Every D-01..D-10 + security + cross-cutting behavior has a named test referenced in 03-VALIDATION.md"
    - "Tests compile and fail/skip cleanly against the not-yet-implemented generator (RED state)"
  artifacts:
    - path: "internal/releasedocs/generators/apidocs/apidocs_test.go"
      provides: "table-driven test scaffold covering D-01..D-10 + SSRF + raw passthrough"
      contains: "fakeFetcher"
    - path: "internal/releasedocs/generators/apidocs/testdata/petstore_v3.yaml"
      provides: "OAS 3.0 fixture"
      min_lines: 10
    - path: "internal/releasedocs/publishers/pages/pages_apidocs_test.go"
      provides: "TestPublish_APIDocs_Paths + TestIdempotent_APIDocs"
      contains: "TestPublish_APIDocs_Paths"
  key_links:
    - from: "internal/releasedocs/generators/apidocs/apidocs_test.go"
      to: "releasedocs.FileFetcher"
      via: "fakeFetcher implements FetchFileFromRef + vcs.Provider"
      pattern: "FetchFileFromRef"
---

<objective>
Create the Wave 0 test scaffold the whole phase verifies against: the fixtures (Swagger 2.0, OAS 3.0, OAS 3.1, malformed, remote-$ref, oversized), a reusable `fakeFetcher` that implements both `releasedocs.FileFetcher` and `vcs.Provider`, the table-driven test stubs covering every decision in 03-VALIDATION.md, and the pages-publisher apidocs path/idempotency tests.

Purpose: Nyquist compliance — every implementation task in Plans 03-05 maps to a `go test -run` command that must already exist. Tests are authored RED (against the not-yet-built generator) and turn GREEN as the implementation lands.
Output: `apidocs_test.go`, `testdata/*.yaml`, golden directory scaffold, and `pages_apidocs_test.go`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-api-docs-openapi/03-VALIDATION.md
@.planning/phases/03-api-docs-openapi/03-PATTERNS.md
@.planning/phases/03-api-docs-openapi/03-RESEARCH.md

<interfaces>
<!-- From 03-01 (must be complete): releasedocs.KindAPIDocs, Artifact.Filename, MultiGenerator, config.APIDocsConfig -->
<!-- From internal/releasedocs/generators/blog/blog_test.go (analog) -->

releasedocs.FileFetcher (releasedocs.go:135-140) — FetchFileFromRef(ctx, repo, ref, path string) ([]byte, error). Missing file reported as an error; callers apply isMissingFile 404 tolerance (match "404"/"not found").

config.ReleaseDocs.Artifacts.APIDocs (APIDocsConfig) — embeds ArtifactConfig{Enabled, When, Preset, Template} inline + SpecPath string.

ReleaseContext fields the generator reads: Repo, ToRef, Config (config.ReleaseDocs), Provider (vcs.Provider — type-assert FileFetcher).

The apidocs generator (Plan 05) will expose: apidocs.New() *Generator, Kind() == releasedocs.KindAPIDocs, Enabled(cfg, bump), and GenerateMulti(ctx, rc) ([]Artifact, error).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author OpenAPI/Swagger test fixtures</name>
  <files>internal/releasedocs/generators/apidocs/testdata/petstore_v2.yaml, internal/releasedocs/generators/apidocs/testdata/petstore_v3.yaml, internal/releasedocs/generators/apidocs/testdata/petstore_v31.yaml, internal/releasedocs/generators/apidocs/testdata/invalid.yaml, internal/releasedocs/generators/apidocs/testdata/remote_ref.yaml, internal/releasedocs/generators/apidocs/testdata/oversized.yaml</files>
  <read_first>.planning/phases/03-api-docs-openapi/03-RESEARCH.md (§ Recommended Project Structure — testdata listing; § Security Domain — $ref SSRF, oversized OOM); .planning/phases/03-api-docs-openapi/03-VALIDATION.md (Wave 0 Requirements)</read_first>
  <action>Create six minimal but valid fixtures. `petstore_v2.yaml`: Swagger 2.0 (`swagger: "2.0"`) with an info block and at least two tagged paths/operations with parameters. `petstore_v3.yaml`: OAS 3.0.x (`openapi: 3.0.3`) petstore subset — multiple tags, paths, operations, parameters, request/response schemas. `petstore_v31.yaml`: OAS 3.1.x (`openapi: 3.1.0`). `invalid.yaml`: syntactically malformed YAML that fails to parse. `remote_ref.yaml`: a valid OAS 3.0 doc whose a `$ref` points at a remote URL (e.g. `https://example.com/schemas/Pet.yaml`) AND a file `$ref` (e.g. `./external.yaml#/X`) — used to assert these are NOT resolved when AllowRemoteReferences/AllowFileReferences are false. `oversized.yaml`: a spec whose raw byte length exceeds the 5 MB max-size guard (generate padding via repeated description text or a comment block) — used to assert the OOM guard skips it. Keep v2/v3/v31 small enough that golden Markdown stays readable. These must be deterministic (no timestamps).</action>
  <verify>
    <automated>test -s internal/releasedocs/generators/apidocs/testdata/petstore_v3.yaml && test -s internal/releasedocs/generators/apidocs/testdata/petstore_v2.yaml && test -s internal/releasedocs/generators/apidocs/testdata/petstore_v31.yaml && test -s internal/releasedocs/generators/apidocs/testdata/remote_ref.yaml && [ "$(wc -c < internal/releasedocs/generators/apidocs/testdata/oversized.yaml)" -gt 5242880 ]</automated>
  </verify>
  <acceptance_criteria>All six fixtures exist; v2 declares `swagger: "2.0"`, v3 `openapi: 3.0.x`, v31 `openapi: 3.1.0`; invalid.yaml is unparseable; remote_ref.yaml contains both a remote URL `$ref` and a file `$ref`; oversized.yaml exceeds 5 MB (5242880 bytes).</acceptance_criteria>
  <done>Fixtures cover all supported versions plus the malformed, remote-ref, and oversized failure paths.</done>
</task>

<task type="auto">
  <name>Task 2: Build the apidocs test scaffold (fakeFetcher + table-driven stubs)</name>
  <files>internal/releasedocs/generators/apidocs/apidocs_test.go</files>
  <read_first>internal/releasedocs/generators/blog/blog_test.go (package decl, fixture builder, table-driven + t.Parallel patterns, determinism test); .planning/phases/03-api-docs-openapi/03-PATTERNS.md (apidocs_test.go section — fakeFetcher stub, fixtureAPIDocsRC); .planning/phases/03-api-docs-openapi/03-VALIDATION.md (Per-Task Verification Map — exact test names)</read_first>
  <action>Create `package apidocs_test`. Define a `fakeFetcher` struct holding `files map[string][]byte` (path→content; absent path returns an error containing "404 not found" for isMissingFile tolerance) that implements `releasedocs.FileFetcher` AND the minimal `vcs.Provider` surface so it can be set as `ReleaseContext.Provider` (embed an existing minimal/fake provider from the releasedocs test helpers if one exists, otherwise stub the required methods). Add a `fixtureAPIDocsRC(files map[string][]byte, specPath, when string, enabled bool, bump)` helper building a `releasedocs.ReleaseContext` with `Config.Artifacts.APIDocs` set and `Provider: &fakeFetcher{...}`. Author the following test functions, each `t.Parallel()`, with the EXACT names from 03-VALIDATION.md so the per-task verify commands resolve: `TestAPIDocs_Kind`, `TestEnabled` (table: enabled=false→false, enabled+when=always default coercion→true across bumps), `TestDiscoverSpec` (table: specPath set→exact fetch no fallback; empty→ordered fallback first-hit-wins; all 404→error), `TestGenerate_FetchesAtToRef`, `TestGenerate_ThreeArtifacts` (assert 3 artifacts with Filename openapi.yaml / api-reference.html / api-reference.md), `TestGenerate_RawSpecPassthrough` (openapi.yaml content == fetched bytes), `TestGenerate_Swagger2`, `TestGenerate_OAS3`, `TestGenerate_OAS31`, `TestGenerate_NoSpec_Skips`, `TestGenerate_ParseFailure_Skips`, `TestGenerate_ValidationFailure_Skips`, `TestGenerate_UnsupportedVersion_Skips`, `TestGenerate_NoRemoteRef` (remote_ref.yaml → remote/file $ref NOT resolved; assert no outbound resolution / the unresolved ref survives or skip-on-unresolvable), `TestBuildRedocHTML_Deterministic` (same spec+bundle → byte-identical), `TestBuildRedocHTML_NoCDN` (output contains no `cdn.redoc.ly` and no external `src=`), `TestRenderMarkdown_Golden` (compares against testdata/golden/markdown_v3.golden, with an `-update`-style guard). For tests whose implementation does not yet exist (Plans 03-05), it is acceptable to author them to fail or to gate behind a build that 03-05 satisfies — but they MUST compile and reference the real symbols (apidocs.New, GenerateMulti). Add a `golden` update mechanism (flag var, default false). No subjective assertions.</action>
  <verify>
    <automated>go vet ./internal/releasedocs/generators/apidocs/... 2>&1 | head -5; go test -run TestAPIDocs_Kind ./internal/releasedocs/generators/apidocs/... 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>`apidocs_test.go` compiles (package builds even before full generator impl, referencing apidocs.New/Kind/Enabled/GenerateMulti); `fakeFetcher` implements FileFetcher + vcs.Provider; every test name in 03-VALIDATION.md's Per-Task Verification Map is present; golden update guard exists. RED state acceptable for not-yet-implemented behaviors.</acceptance_criteria>
  <done>Full table-driven scaffold exists with every validation-mapped test name, ready to turn green as Plans 03-05 land.</done>
</task>

<task type="auto">
  <name>Task 3: Pages-publisher apidocs path + idempotency tests</name>
  <files>internal/releasedocs/publishers/pages/pages_apidocs_test.go</files>
  <read_first>internal/releasedocs/publishers/pages/pages.go (Publish loop after Plan 01's Filename change); existing pages test file in the same package (recording fake BranchCommitter pattern); .planning/phases/03-api-docs-openapi/03-VALIDATION.md (Cross-cut + Idempotency rows)</read_first>
  <action>Add `TestPublish_APIDocs_Paths`: build three artifacts of Kind `releasedocs.KindAPIDocs` with Filenames `openapi.yaml`, `api-reference.html`, `api-reference.md` and assert the recording BranchCommitter receives UpsertFile at `{dir}/releases/{toRef}/openapi.yaml`, `.../api-reference.html`, `.../api-reference.md` respectively (reuse the existing package's recording fake; if none is exported, mirror its construction from the existing pages test). Add `TestIdempotent_APIDocs`: call Publish twice with identical inputs and assert both runs hit the same set of UpsertFile paths (no duplicate/extra paths). Reuse the existing test's recording BranchCommitter + ReleaseContext fixture so backward-compat coverage is unchanged.</action>
  <verify>
    <automated>go test -race -run 'TestPublish_APIDocs_Paths|TestIdempotent_APIDocs' ./internal/releasedocs/publishers/pages/...</automated>
  </verify>
  <acceptance_criteria>Both tests compile and pass against the Plan 01 pages-publisher change; `.yaml`/`.html`/`.md` artifacts route to their Filename-derived deterministic paths; two Publish calls produce identical path sets.</acceptance_criteria>
  <done>Pages publisher apidocs routing + idempotency proven; existing pages suite still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixture spec → parser under test | The remote_ref + oversized fixtures are the negative-test vehicles for the SSRF and OOM mitigations implemented in Plan 03. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Information Disclosure | remote/file `$ref` resolution | mitigate | `remote_ref.yaml` fixture + `TestGenerate_NoRemoteRef` assert the parser does NOT resolve remote/file refs (the actual guard lands in Plan 03). |
| T-03-04 | Denial of Service | oversized spec | mitigate | `oversized.yaml` (>5 MB) fixture exercises the max-size skip guard (implemented in Plan 03). |
| T-03-01 | Tampering | pages path from Filename | mitigate | `TestPublish_APIDocs_Paths` asserts paths stay under `{dir}/releases/{toRef}/` (traversal guard from Plan 01). |
</threat_model>

<verification>
- `go vet ./internal/releasedocs/generators/apidocs/...` clean (package compiles).
- `go test -race ./internal/releasedocs/publishers/pages/...` green (Task 3 + existing backward-compat).
- Every test name listed in 03-VALIDATION.md's Per-Task Verification Map exists in the scaffold.
</verification>

<success_criteria>
- Six fixtures present (v2, v3, v31, invalid, remote_ref, oversized).
- `fakeFetcher` drives table-driven tests with no real VCS.
- All validation-mapped test names authored; pages apidocs path + idempotency tests pass.
</success_criteria>

<output>
Create `.planning/phases/03-api-docs-openapi/03-02-SUMMARY.md` when done.
</output>
