---
phase: 02-webhook-auto-trigger-state
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - db/migrations/0006_release_docs_state.sql
  - internal/releasedocs/state/state.go
  - internal/releasedocs/state/state_test.go
autonomous: false
requirements:
  - REQ-release-docs-idempotency
user_setup: []

must_haves:
  truths:
    - "A new release_docs_state table exists, keyed uniquely on (provider, repo_full_name, to_tag, artifact_kind)"
    - "Migration 0006 round-trips up→down→up cleanly against pg16"
    - "A nil StateStore is safe to call (no-op), matching the findings.Store nil-tolerance contract"
    - "Recording the same (provider, repo, to_tag, artifact_kind) twice updates external_id and updated_at, not a duplicate row"
  artifacts:
    - path: "db/migrations/0006_release_docs_state.sql"
      provides: "release_docs_state table with goose Up/Down"
      contains: "CREATE TABLE release_docs_state"
    - path: "internal/releasedocs/state/state.go"
      provides: "DB-backed StateStore (Record/Lookup), nil-tolerant, decoupled from releasedocs"
      contains: "func New"
  key_links:
    - from: "internal/releasedocs/state/state.go"
      to: "release_docs_state table"
      via: "INSERT ON CONFLICT DO UPDATE"
      pattern: "ON CONFLICT"
---

<objective>
Create the DB-backed idempotency layer for release docs: a goose migration `0006_release_docs_state.sql` for a `release_docs_state` table keyed on `(provider, repo_full_name, to_tag, artifact_kind)`, plus a nil-tolerant `internal/releasedocs/state` store mirroring the `findings.Store` pattern (DB-backed via pgxpool; nil = stateless/marker mode).

Purpose: REQ-release-docs-idempotency Phase 2 extension — re-runs must edit in place even across process restarts (not just within one process via markers). The store is the persistence layer; the dispatcher wiring that consumes it lands in 02-06.

Output: One goose migration that round-trips up→down→up; a `state.Store` with `Record` and `Lookup` methods that no-op on a nil receiver. CRITICAL: the store package takes the artifact kind as a plain `string` (NOT releasedocs.ArtifactKind) so it imports NOTHING from `internal/releasedocs` — this prevents an import cycle when the dispatcher (package releasedocs) imports this sub-package in 02-06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-webhook-auto-trigger-state/02-RESEARCH.md

<interfaces>
<!-- Existing patterns to mirror. -->

From db/migrations/0003_posted_state.sql (the migration pattern: BIGSERIAL PK, UNIQUE composite, INDEX on lookup tuple, goose Up/Down StatementBegin/End):
```sql
CREATE TABLE posted_findings (
    id BIGSERIAL PRIMARY KEY,
    provider TEXT NOT NULL,
    repo_full_name TEXT NOT NULL,
    pr_number BIGINT NOT NULL,
    ...
    UNIQUE (provider, repo_full_name, pr_number, fingerprint)
);
```

From internal/findings/findings.go (nil-tolerance contract to mirror):
```go
// A nil Store is safe to call against; methods become no-ops returning zero
// values so the dispatcher's hot path doesn't need nil-checks.
type Store struct { pool *pgxpool.Pool; mem *memoryStore }
func New(pool *pgxpool.Pool) *Store
```

Artifact-kind string values stored in the artifact_kind column (kept as plain strings here — the store does NOT import releasedocs):
```
"changelog", "release_notes", "blog"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0006_release_docs_state.sql</name>
  <files>db/migrations/0006_release_docs_state.sql</files>
  <read_first>
    - db/migrations/0003_posted_state.sql (exact goose Up/Down + UNIQUE composite + INDEX pattern to copy)
    - db/migrations/0005_*.sql (confirm 0006 is the next free number; list db/migrations/ first)
    - .planning/phases/02-webhook-auto-trigger-state/02-RESEARCH.md (Pattern 5 — the exact target DDL)
  </read_first>
  <action>
    Create db/migrations/0006_release_docs_state.sql following the 0003 goose format exactly (`-- +goose Up` / `-- +goose StatementBegin` ... `-- +goose StatementEnd`, then `-- +goose Down` block dropping the table).

    Table `release_docs_state` columns: `id BIGSERIAL PRIMARY KEY`, `org_id TEXT` (multi-tenancy, TEXT not FK — same as posted_findings), `provider TEXT NOT NULL`, `repo_full_name TEXT NOT NULL`, `to_tag TEXT NOT NULL`, `artifact_kind TEXT NOT NULL`, `external_id TEXT`, `published_at TIMESTAMPTZ NOT NULL DEFAULT now()`, `updated_at TIMESTAMPTZ NOT NULL DEFAULT now()`. Add `UNIQUE (provider, repo_full_name, to_tag, artifact_kind)` and `CREATE INDEX release_docs_state_repo_idx ON release_docs_state (provider, repo_full_name, to_tag)`. The Down block: `DROP TABLE IF EXISTS release_docs_state;`.

    Confirm the file number 0006 is unused before writing (list db/migrations/). Forward-only convention applies but goose requires the Down block for CI's up→down→up round-trip.
  </action>
  <verify>
    <automated>grep -c "CREATE TABLE release_docs_state" db/migrations/0006_release_docs_state.sql</automated>
  </verify>
  <acceptance_criteria>
    - File db/migrations/0006_release_docs_state.sql exists and is the highest-numbered migration
    - `grep -c "UNIQUE (provider, repo_full_name, to_tag, artifact_kind)" db/migrations/0006_release_docs_state.sql` returns 1
    - `grep -c "DROP TABLE IF EXISTS release_docs_state" db/migrations/0006_release_docs_state.sql` returns 1
    - File contains both `-- +goose Up` and `-- +goose Down` markers
  </acceptance_criteria>
  <done>Migration 0006 created with the keyed table, unique composite, lookup index, and a clean Down block.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: confirm migration 0006 round-trips up→down→up</name>
  <action>Confirm the migration applies, rolls back, and re-applies against a live Postgres (the exact CI migrations-job gate). This requires a reachable pg16 instance, so it is a human-verified checkpoint rather than an in-sandbox automated step.</action>
  <what-built>Migration 0006_release_docs_state.sql — the release_docs_state idempotency table. CI runs migrations up→down→up against pgvector/pg16, so the round-trip must be confirmed against a live Postgres before the worker (02-06) depends on the table.</what-built>
  <how-to-verify>
    1. Ensure a Postgres is reachable: `make dev-up` (or set DATABASE_URL to an existing pg16 instance).
    2. Run `make migrate` — expect 0006 to apply cleanly (no error).
    3. Run `make migrate-down` — expect 0006 to roll back (table dropped).
    4. Run `make migrate` again — expect 0006 to re-apply cleanly (proves up→down→up round-trip, the exact CI gate).
    5. Optionally: `psql $DATABASE_URL -c '\d release_docs_state'` and confirm the UNIQUE constraint on (provider, repo_full_name, to_tag, artifact_kind).
  </how-to-verify>
  <resume-signal>Type "approved" if up→down→up succeeded, or describe the migration error.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: state.Store — DB-backed, nil-tolerant Record/Lookup (kind as string)</name>
  <files>internal/releasedocs/state/state.go, internal/releasedocs/state/state_test.go</files>
  <read_first>
    - internal/findings/findings.go (New(pool) constructor; nil-receiver no-op pattern; pgx query style with pgxpool)
    - db/migrations/0006_release_docs_state.sql (column names and the ON CONFLICT target from Task 1)
  </read_first>
  <behavior>
    - Test nil receiver: a nil *Store calling Record returns nil; calling Lookup returns a zero result and nil error (no panic). This is the stateless-mode contract.
    - Test key signature: Record accepts (org, provider, repoFullName, toTag, kind, externalID string) — kind is a plain string, not a releasedocs type.
    - Test (DB-gated): recording the same composite key twice updates external_id and updated_at and does NOT create a second row (ON CONFLICT DO UPDATE). If a live DB is not available, cover the nil-tolerance/signature in pure unit tests and gate the DB round-trip behind the migration checkpoint above; skip the DB test with t.Skip when DATABASE_URL is unset.
  </behavior>
  <action>
    Create package `state` at internal/releasedocs/state/state.go. Define `type Store struct { pool *pgxpool.Pool }` and `func New(pool *pgxpool.Pool) *Store`.

    CRITICAL isolation: the package MUST NOT import internal/orchestrator, internal/riverq, OR internal/releasedocs. Take the artifact kind as a plain `string` parameter (NOT releasedocs.ArtifactKind) so there is zero coupling to the releasedocs package — this is what lets the dispatcher (which IS package releasedocs) import this sub-package in 02-06 without a cycle. Imports limited to: context, fmt, github.com/jackc/pgx/v5, github.com/jackc/pgx/v5/pgxpool.

    Methods (both nil-receiver-safe — guard `if s == nil || s.pool == nil { return ... }` at the top):
    - `Record(ctx context.Context, org, provider, repoFullName, toTag, kind, externalID string) error` — `INSERT INTO release_docs_state (org_id, provider, repo_full_name, to_tag, artifact_kind, external_id) VALUES ($1,$2,$3,$4,$5,$6) ON CONFLICT (provider, repo_full_name, to_tag, artifact_kind) DO UPDATE SET external_id = EXCLUDED.external_id, updated_at = now()`.
    - `Lookup(ctx context.Context, provider, repoFullName, toTag, kind string) (externalID string, found bool, err error)` — SELECT external_id by the composite key (parameterized).

    Docstring every exported symbol (revive `exported` is on). Write state_test.go covering nil-receiver no-op at minimum; add a DB round-trip test guarded by DATABASE_URL (t.Skip otherwise).
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/releasedocs/state/...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ON CONFLICT (provider, repo_full_name, to_tag, artifact_kind)" internal/releasedocs/state/state.go` returns 1
    - `grep -c "internal/orchestrator\|internal/riverq\|internal/releasedocs\"" internal/releasedocs/state/state.go` returns 0 (full isolation, no cycle risk)
    - The Record signature uses `kind string` (no ArtifactKind type) — `grep -c "ArtifactKind" internal/releasedocs/state/state.go` returns 0
    - A test named for nil-receiver behavior passes: nil *Store Record returns nil and Lookup returns (\"\", false, nil)
    - `go test -race -count=1 ./internal/releasedocs/state/...` passes (DB round-trip test skips cleanly when DATABASE_URL unset)
  </acceptance_criteria>
  <done>state.Store with Record/Lookup exists, is nil-tolerant, uses ON CONFLICT DO UPDATE on the composite key, takes kind as a plain string, imports nothing from releasedocs/orchestrator/riverq, and tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| webhook payload → DB columns | provider/repo/to_tag values reach SQL parameters; must be parameterized, never interpolated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering (SQL injection) | state.Store Record/Lookup | mitigate | Use pgx parameterized queries ($1,$2,...) exclusively; never string-concatenate to_tag/repo into SQL |
| T-02-04 | Repudiation | release_docs_state rows | accept | published_at/updated_at timestamps recorded; multi-tenant org_id stored; no audit-log requirement at ASVS L1 |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs; pgx/v5 already in go.mod (RESEARCH.md: zero new dependencies) |
</threat_model>

<verification>
- Migration up→down→up confirmed via the blocking checkpoint (matches CI's migrations job)
- `go test -race -count=1 ./internal/releasedocs/state/...` green
- `make lint` clean (every exported state symbol documented)
- state package imports nothing from releasedocs (verified by grep — guarantees no cycle in 02-06)
</verification>

<success_criteria>
- release_docs_state table exists, keyed on (provider, repo_full_name, to_tag, artifact_kind), migration round-trips up→down→up
- state.Store is nil-tolerant and records via ON CONFLICT DO UPDATE, taking kind as a plain string
- No orchestrator/riverq/releasedocs import in the state package (no cycle risk)
</success_criteria>

<output>
Create `.planning/phases/02-webhook-auto-trigger-state/02-02-SUMMARY.md` when done
</output>
